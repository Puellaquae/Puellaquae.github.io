<!DOCTYPE html>
<html lang="zh-Hans">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{metadata['title']|e}}">
    <title>{{metadata["title"]|e}}</title>
    <link rel="stylesheet" href="{{metadata['relativeRoot']}}/css/style.css" />
    <link rel="stylesheet" href="{{metadata['relativeRoot']}}/css/codecolor.css" />
</head>

<body>
    <header class="header">
        <section sub="{{metadata['subtype']}}" class="navbar-left title">
        </section>
        <section class="navbar-right">
            {% for article in articles %}
            {%- if article["specialPosition"].includes("navbar-article") -%}
            <a href="{{metadata['relativeRoot']}}/{{article['outdir']}}/{{article['outfilename']}}">{{article["title"]|e}}</a>
            {% endif %}
            {%- endfor -%}
        </section>
    </header>
    <div class="container">
        <div class="archive {%- if metadata['useIndent'] %} indent {%- endif %}">
            <h1>{{metadata["title"]|e}}</h1>
            <div class="modified date">
                {{metadata["modifyDate"].toLocaleDateString("zh-CN", {year:"numeric", month:"long", day:"numeric"})}}
            </div>
            <div class="create date">
                {{metadata["createDate"].toLocaleDateString("zh-CN", {year:"numeric", month:"long", day:"numeric"})}}
            </div>
            {% for tag in metadata["tags"] %}
            <span class="meta">{{tag}}</span>
            {% endfor %}
            <div class="entry">
                {{content}}
            </div>
        </div>
    </div>

    <div class="footer">
        <p rel="license"><a rel="license" href="http://creativecommons.org/licenses/by-nc/4.0/">CC BY-NC 4.0</a></p>
        <p rel="contact">
            <a href="mailto:shentukeqin@hotmail.com">Mail</a>
            <a href="https://github.com/Puellaquae">Github</a>
        </p>
    </div>
</body>

</html>