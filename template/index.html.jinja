<!DOCTYPE html>
<html lang="zh-Hans">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="google-site-verification" content="63QLzDmTkV6MqDn2gHkIQKpXojnEXrtQWBF3NgmMXq8" />
    <title>Puelloc Blog</title>
    <link rel="stylesheet" href="css/style.css" />
</head>

<body>
    <input type="checkbox" id="timeLine" hidden>
    <header class="header">
        <section sub="" class="navbar-left title"></section>
        <section class="navbar-right">
            <label for="timeLine" tabindex="0"></label>
            {% for article in articles %}
            {%- if article["specialPosition"].includes("navbar-index") -%}
            <a href="{{article['outdir']}}/{{article['outfilename']}}">{{article["specialShowName"]|e}}</a>
            {% endif %}
            {%- endfor -%}
        </section>
    </header>
    <div class="container">
        <div class="posts">
            {% for article in articles %}
            {%- if article["hideIndex"] != "html" and article["hideIndex"] != "both" -%}
            <div class="post">
                <a href="{{article['outdir']}}/{{article['outfilename']}}">{{article["titleHtml"]}}</a>
                <div class="date" title="最后修改于 {{article['modifyDate'].toLocaleDateString('zh-CN', {year:'numeric', month:'2-digit', day:'2-digit'})}}">{{article["createDate"].toLocaleDateString("zh-CN", {year:"numeric", month:"2-digit", day:"2-digit"})}}</div>
                {%- for tag in article["tags"] %}
                <label class="meta">{{tag}}</label>
                {%- endfor %}
            </div>
            {% endif %}
            {%- endfor -%}
        </div>
    </div>
    <div class="footer">
    </div>
</body>

</html>